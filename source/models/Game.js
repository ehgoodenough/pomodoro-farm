export default class Game {
    constructor(game) {
        this.score = 0

        this.items = [
            {label: "5x potato seeds"},
            {label: "watering can"}
        ]
    }
    update(delta) {
        // this.slabs.forEach((slab) => {
        //     slab.update(delta)
        // })
    }
}
